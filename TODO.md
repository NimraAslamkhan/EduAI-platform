# TODO - GitHub upload cleanup

- [ ] Create .gitignore to avoid committing secrets / env files.
- [ ] Ensure only useful project files are included (keep app.py, src/, scripts/, requirements.txt, README.md, .streamlit/).
- [ ] Run git status to verify what will be committed.
- [ ] Remove any previously-tracked sensitive files from git index (if needed).
- [ ] Commit changes and push to GitHub.

